import { _ as _export_sfc, o as openBlock, c as createElementBlock, a as createStaticVNode } from "./app.35c0796e.js";
const __pageData = JSON.parse('{"title":"Process Management","description":"","frontmatter":{},"headers":[{"level":2,"title":"Front-End / Shuttle.ProcessManagement.Vue","slug":"front-end-shuttle-processmanagement-vue","link":"#front-end-shuttle-processmanagement-vue","children":[]},{"level":2,"title":"Database","slug":"database","link":"#database","children":[]},{"level":2,"title":"Process","slug":"process","link":"#process","children":[]}],"relativePath":"guide/patterns/process-management.md"}');
const _sfc_main = { name: "guide/patterns/process-management.md" };
const _hoisted_1 = /* @__PURE__ */ createStaticVNode('<h1 id="process-management" tabindex="-1">Process Management <a class="header-anchor" href="#process-management" aria-hidden="true">#</a></h1><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Remember that you can download the samples from the <a href="https://github.com/Shuttle/Shuttle.Esb.Samples" target="_blank">GitHub repository</a>.</p></div><p>Our sample case is a fictitious online book store where you can order books. An order allows you 20 seconds to cancel the order before it is accepted.</p><p>The process management sample represents something closer to a real-world scenario where one would have a front-end integrating with a web-api. The web-api issues commands, via messaging/queues, that are processed by the process managers.</p><p>In this sample the front-end is a static <a href="https://vuejs.org/" target="_blank" rel="noreferrer">Vue.js</a>. The REST API is a <code>dotnet</code> web-api. There are three physical implementations of the same logical process manager to demonstrate the various options, and the read-model is kept updated using CQRS with system messages. For the event-sourcing side one could just as easily use event processing to update the read model but since the system event messages are being processed it is re-used for the event sourcing implementation also.</p><p>Once you have opened the <code>Shuttle.ProcessManagement.sln</code> solution in Visual Studio set the following projects as startup projects:</p><ul><li>Shuttle.EMailSender.Server</li><li>Shuttle.Invoicing.Server</li><li>Shuttle.Ordering.Server</li><li>Shuttle.Process.Custom.Server</li><li>Shuttle.Process.CustomES.Server</li><li>Shuttle.Process.ESModule.Server</li><li>Shuttle.Process.QueryServer</li><li>Shuttle.ProcessManagement.WebApi</li></ul><p>Since the subscribers register the relevant subscriptions the publishers need to know about these. After starting up all the projects please stop everything and re-start in order for the publishers to pick up the subscriptions.</p><h2 id="front-end-shuttle-processmanagement-vue" tabindex="-1">Front-End / Shuttle.ProcessManagement.Vue <a class="header-anchor" href="#front-end-shuttle-processmanagement-vue" aria-hidden="true">#</a></h2><p>In order to run the <code>Vue</code> front-end the simplest would be to open up the folder in <code>vscode</code> then running <code>npm install</code> in a terminal window follewed by <code>npm run dev</code>.</p><p>You will also need to create and configure a Sql Server database for this sample and remember to all the <code>ConnectionStrings</code> settings in the <code>appsettings.json</code> files to point to your database as the default <code>ConnectionStrings</code> is typically as follows:</p><div class="language-json"><button class="copy"></button><span class="lang">json</span><pre><code><span class="line"><span style="color:#89DDFF;">{</span></span>\n<span class="line"><span style="color:#A6ACCD;">  </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C792EA;">ConnectionStrings</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">{</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;">&quot;</span><span style="color:#FFCB6B;">azure</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">UseDevelopmentStorage=true;</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">,</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;">&quot;</span><span style="color:#FFCB6B;">ProcessManagement</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">server=.;database=ProcessManagement;user id=sa;password=Pass!000</span><span style="color:#89DDFF;">&quot;</span></span>\n<span class="line"><span style="color:#A6ACCD;">  </span><span style="color:#89DDFF;">}</span></span>\n<span class="line"><span style="color:#89DDFF;">}</span></span>\n<span class="line"></span></code></pre></div><h2 id="database" tabindex="-1">Database <a class="header-anchor" href="#database" aria-hidden="true">#</a></h2><p>We need a store for our subscriptions. In this example we will be using <strong>Sql Server</strong>. If you are using docker you can quickly get up-and-running with the following:</p><div class="language-"><button class="copy"></button><span class="lang"></span><pre><code><span class="line"><span style="color:#A6ACCD;">docker run -d --name sql -h sql -e &quot;ACCEPT_EULA=Y&quot; -e &quot;SA_PASSWORD=Pass!000&quot; -e &quot;MSSQL_PID=Express&quot; -p 1433:1433 -v C:\\SQLServer.Data\\:/var/opt/mssql/data mcr.microsoft.com/mssql/server:2019-latest</span></span>\n<span class="line"><span style="color:#A6ACCD;"></span></span></code></pre></div><p>When you reference the <code>Shuttle.Esb.Sql.Subscription</code> package a <code>scripts</code> folder is included in the relevant package folder. Click on the NuGet referenced assembly in the <code>Dependencies</code> and navigate to the package folder (in the <code>Path</code> property) to find the <code>scripts</code> folder.</p><p>The <code>{version}</code> bit will be in a <code>semver</code> format.</p><blockquote><p>Create a new database called <strong>ProcessManagement</strong> and execute the script <code>{provider}\\SubscriptionManagerCreate.sql</code> in the newly created database.</p></blockquote><p>This will create the required structures that the subscription manager will use to store the subcriptions. However, this step is optional as the <code>SqlSubscriptionService</code> implementation will create any required structures. In many cases one would need to create the structures manually, such as in production environments, so the script execution process is included for completeness.</p><p>Similarly, the <code>Shuttle.Recall.Sql.Storage</code> and <code>Shuttle.Recall.Sql.EventProcessing</code> packages contain the following scripts that you should execute against the <code>ProcessManagement</code> database (not optional):</p><ul><li><code>%UserProfile%\\.nuget\\packages\\shuttle.recall.sql.storage\\{version}\\scripts\\{provider}\\EventStoreCreate.sql</code></li><li><code>%UserProfile%\\.nuget\\packages\\shuttle.recall.sql.eventprocessing\\{version}\\scripts\\{provider}\\ProjectionCreate.sql</code></li></ul><p>This will create the relevant structures used by the <code>Shuttle.Recall</code> event-sourcing and projection mechanisms.</p><p>In addition to this the actual process management application also requires some structures that need to be created:</p><blockquote><p>Execute script &#39;{extraction-folder}\\Shuttle.Esb.Samples\\Shuttle.ProcessManagement.scripts\\process-management.sql&#39; in the <code>ProcessManagement</code> database.</p></blockquote><p>You should now be able to run the application.</p><h2 id="process" tabindex="-1">Process <a class="header-anchor" href="#process" aria-hidden="true">#</a></h2><p>Once you add books to your order you can place the order using any of the following processes:</p><ul><li>Custom <ul><li>A hand-rolled process manager storing the process state in custom tables. This is a more traditional approach to data access.</li></ul></li><li>Custom / EventSource <ul><li>A hand-rolled process manager that stored the process state using the <code>Shuttle.Recall</code> event sourcing mechanism directly.</li></ul></li><li>EventSource / Module <ul><li>This uses the <code>Shuttle.Esb.Process</code> module to handle the process state storage for you.</li></ul></li></ul><p>Once you register an order by clicking on the button the <code>RegisterOrderProcess</code> command is immediately sent to the relevant endpoint for processing. An <code>AcceptOrderProcess</code> command is then sent locally (to the same endpoint) but it is deferred for 20 seconds. This allows you time to cancel the order.</p><p>The handler processing the <code>AcceptOrderProcess</code> will simply ignore the message should the process/order have been cancelled in the meantime; else it sends a <code>CreateOrder</code> command to the order processing endpoint. Once the order has been created the process manager receives an <code>OrderCreated</code> event and it then sends a <code>CreateInvoice</code> command.</p><p>When the process manager receives the <code>InvoiceCreated</code> event an e-mail needs to be sent to the customer so a <code>SendEMail</code> command is sent to the e-mail server endpoint. Once the <code>EMailSent</code> event is recevied by the process manager a <code>CompleteOrderProcess</code> command is sent locally that then results in an <code>OrderProcessCompleted</code> event.</p><p>After the process has been completed it is possible to <code>Archive</code> the process. In our sample it simply deletes the order.</p><p>As you can see from all the messages, the process manager is responsible for the interaction between the various business and infrastructure endpoints to ensure that your use-case runs to completion. None of the systems know about the inner working of another and may be re-used in different processes.</p>', 33);
const _hoisted_34 = [
  _hoisted_1
];
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, _hoisted_34);
}
const processManagement = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  processManagement as default
};
