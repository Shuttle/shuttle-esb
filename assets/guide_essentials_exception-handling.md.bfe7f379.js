import { _ as _export_sfc, c as createElementBlock, o as openBlock, a as createStaticVNode } from "./app.c706ac22.js";
const __pageData = '{"title":"Exception Handling","description":"","frontmatter":{},"headers":[],"relativePath":"guide/essentials/exception-handling.md"}';
const _sfc_main = {};
const _hoisted_1 = /* @__PURE__ */ createStaticVNode('<h1 id="exception-handling" tabindex="-1">Exception Handling <a class="header-anchor" href="#exception-handling" aria-hidden="true">#</a></h1><p>When an exception occurs within a pipeline an <code>OnPipelineException</code> event is raised on the pipeline and any observers that have hooked onto the event will be called:</p><div class="language-c#"><pre><code><span class="line"><span style="color:#C792EA;">public</span><span style="color:#A6ACCD;"> </span><span style="color:#F78C6C;">class</span><span style="color:#A6ACCD;"> </span><span style="color:#FFCB6B;">ReceiveExceptionObserver</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#FFCB6B;">IPipelineObserver</span><span style="color:#89DDFF;">&lt;</span><span style="color:#FFCB6B;">OnPipelineException</span><span style="color:#89DDFF;">&gt;</span></span>\n<span class="line"><span style="color:#89DDFF;">{</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#C792EA;">public</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">void</span><span style="color:#A6ACCD;"> </span><span style="color:#82AAFF;">Execute</span><span style="color:#89DDFF;">(</span><span style="color:#FFCB6B;">OnPipelineException</span><span style="color:#A6ACCD;"> </span><span style="color:#FFCB6B;">pipelineEvent</span><span style="color:#89DDFF;">)</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;">{</span></span>\n<span class="line"><span style="color:#A6ACCD;">        </span><span style="color:#89DDFF;font-style:italic;">if</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">(</span><span style="color:#A6ACCD;">pipelineEvent</span><span style="color:#89DDFF;">.</span><span style="color:#A6ACCD;">Pipeline</span><span style="color:#89DDFF;">.</span><span style="color:#A6ACCD;">ExceptionHandled</span><span style="color:#89DDFF;">)</span><span style="color:#A6ACCD;"> </span><span style="color:#676E95;font-style:italic;">// &lt;-- set by calling MarkExceptionHandled</span></span>\n<span class="line"><span style="color:#A6ACCD;">        </span><span style="color:#89DDFF;">{</span></span>\n<span class="line"><span style="color:#A6ACCD;">            </span><span style="color:#89DDFF;font-style:italic;">return</span><span style="color:#89DDFF;">;</span></span>\n<span class="line"><span style="color:#A6ACCD;">        </span><span style="color:#89DDFF;">}</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#A6ACCD;">        pipelineEvent</span><span style="color:#89DDFF;">.</span><span style="color:#A6ACCD;">Pipeline</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">MarkExceptionHandled</span><span style="color:#89DDFF;">();</span><span style="color:#A6ACCD;"> </span><span style="color:#676E95;font-style:italic;">// &lt;-- sets ExceptionHandled to true</span></span>\n<span class="line"><span style="color:#A6ACCD;">        pipelineEvent</span><span style="color:#89DDFF;">.</span><span style="color:#A6ACCD;">Pipeline</span><span style="color:#89DDFF;">.</span><span style="color:#82AAFF;">Abort</span><span style="color:#89DDFF;">();</span><span style="color:#A6ACCD;"> </span><span style="color:#676E95;font-style:italic;">// &lt;-- prevents further processing of the pipeline</span></span>\n<span class="line"><span style="color:#A6ACCD;">    </span><span style="color:#89DDFF;">}</span></span>\n<span class="line"><span style="color:#89DDFF;">}</span></span>\n<span class="line"></span></code></pre></div><p>Typically you would not respond to the <code>OnPipelineException</code> event but you need to be cognizant of how it affects the message processing.</p><p>The more interesting bit is that the first order of business is to determine whether or not to retry the message. If the exception is of type <code>UnrecoverableHandlerException</code> the message will not be retried and will be immediately moved to the <code>ErrorQueue</code>. This means that when you are able to determine that the message will never be processed correctly, but you do not want to discard it outright, you can throw this exception and the message will be moved directly to the <code>ErrorQueue</code>.</p><p>If the exception is not of type <code>UnrecoverableHandlerException</code> the implementation of the <code>IServiceBusPolicy</code> on the <code>ServiceBusConfiguration</code> is used to invoke the relevant method. The <code>DefaultServiceBusPolicy</code> makes use of the <code>serviceBus</code> settings in the application configuration file to determine the &#39;maximumFailureCount&#39;. Should the number of failed messages still be within this count the message will be retried and duration to wait until the next retry is determined by using the <code>durationToIgnoreOnFailure</code> value.</p><p>Should the message be retried the exception message is added to the <code>FailureMessages</code> collection of the <code>TransportMessage</code> and the transport message is re-enqueued, moving it to the end of the queue and the <code>IgnoreTillDate</code> property of the <code>TransportMessage</code> is also set to the duration to wait before retrying.</p><p><strong>Note</strong>: this is where you may experience some serious queue thrashing if you do not have a <code>DeferredQueue</code> configured for your inbox. When the inbox processor dequeues the message it check to see whether it can be processed by checking the <code>IgnoreTillDate</code> value. If it cannot be processed it is moved to the <code>DeferredQueue</code> if there is one; else it is simply re-enqueued in the inbox. Since the inbox is responsible for processing messages as quickly as possible this processing will continue until the message becomes available for processing. Given that there is a message to process in the inbox no backing-off will occur. Please always use a deferred queue for anything that will be executed in a production environment.</p>', 8);
const _hoisted_9 = [
  _hoisted_1
];
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, _hoisted_9);
}
var exceptionHandling = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export { __pageData, exceptionHandling as default };
